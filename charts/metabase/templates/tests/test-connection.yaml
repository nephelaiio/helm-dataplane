apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dataplane.fullname" . }}-test-connection"
  labels:
    {{- include "dataplane.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dataplane.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
